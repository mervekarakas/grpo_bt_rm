export DATASET="${DATASET:-/data/$USER/datasets/grpo_bt_train_score100_v1.jsonl}"
export OUT_DIR="${OUT_DIR:-/data/$USER/experiments/grpo_bt_rm/expB_score100_T10_C6_posclip}"

export PLUGIN="${PLUGIN:-src/grpo_bt_rm/training/reward_plugins/bt_baseline.py}"
export REWARD_NAME="${REWARD_NAME:-bt_pointwise_baseline}"

export BT_SCORE_PARSER="${BT_SCORE_PARSER:-score100_first}"
export BT_DELTA_TEMP="${BT_DELTA_TEMP:-10}"
export BT_DELTA_CLIP="${BT_DELTA_CLIP:-6}"
export BT_DELTA_NEG_CLIP="${BT_DELTA_NEG_CLIP:-0}"
export BT_REWARD_SCALE="${BT_REWARD_SCALE:-1}"

export EVAL_PROMPT="${EVAL_PROMPT:-score100_v1}"
export EVAL_PARSER="${EVAL_PARSER:-score100_first}"
export EVAL_BT_TEMP="${EVAL_BT_TEMP:-10}"
export EVAL_UNC_LOW="${EVAL_UNC_LOW:-20}"
export EVAL_UNC_HIGH="${EVAL_UNC_HIGH:-80}"
